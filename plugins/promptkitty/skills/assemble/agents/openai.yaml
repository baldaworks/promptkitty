interface:
  display_name: "PromptKitty Assemble"
  short_description: "Assemble raw and interactive PromptKit prompts"
  default_prompt: "Use $promptkitty:assemble to select and assemble a PromptKit prompt for my task."
