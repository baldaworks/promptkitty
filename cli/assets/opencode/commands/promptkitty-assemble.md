---
description: Assemble a task-specific prompt with PromptKitty.
---

Load the `promptkitty-assemble` skill and follow its assembly workflow exactly.
Treat the following as the user's task:

$ARGUMENTS
